// if
// if.. else..
// if.. else if..

// Comparison operators
// >, <, >=, <=, ==, !=, === (it also checks for type)

const balance = 1000;
if(balance > 500) console.log("test"); // implicit scope
// if(balance > 500) console.log("test"), console.log("test"); // don't do this. Not a good code.

// you can also use &&, ||